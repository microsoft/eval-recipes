timeout: 5400
required_env_vars:
  - ANTHROPIC_API_KEY
task_info:
  difficulty: medium
  non_deterministic_evals: true
  categories:
    - cli_tool
    - writing
test_command: uv run --no-project /project/test.py
